---
title: "Final Project - Job Recommender System"
author: "Christina Valore, Juliann McEachern, & Rajwant Mishra"
date: "July 17, 2019"
output: 
  html_document:
    theme: flatly
    highlight: pygments
    toc: true
    toc_depth: 2
    toc_float: true
    df_print: paged
    code_folding: hide
---

# Project Overview

Insert Project Purpose Here.

#### Dependencies

```{r}
library(data.table)
```

#### Import data 

Use `fread` function to read tsv files. 
```{r}
apps<-as.data.frame(fread("data/apps.tsv"))
users<-as.data.frame(fread("data/users.tsv"))
user_history<-as.data.frame(fread("data/user_history.tsv"))
window_dates<-as.data.frame(fread("data/window_dates.tsv"))
jobs <-untar("tsv.tar.gz",files="data/jobs.tsv")
jobs <- as.data.frame(fread(jobs))
```

```{r}
window_dates
```

